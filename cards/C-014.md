# C-014 — White Frosted Glass Dock & Settings Passcode Display

status: done
deps: C-013

## Scope

1. Chuyển đổi Floating Bottom Dock (`src/components/BottomNav.tsx`) sang phong cách **White Frosted Glassmorphism** (`bg-white/90 backdrop-blur-2xl border-white/80 shadow-xl`).
2. Cập nhật `src/components/SettingsModal.tsx`:
   - Hiển thị Mã PIN phòng (`#839201`) kèm nút Copy 1 chạm.
   - Hiển thị Mật khẩu phòng (`8899`) kèm nút Ẩn/Hiện (Eye toggle) và nút Copy 1 chạm.
   - Xóa bỏ hoàn toàn Form nhập Gemini API Key khỏi giao diện.
3. Cập nhật `src/index.css` với lớp `.glass-dock-white`.

## Allowed files

- `src/components/BottomNav.tsx`
- `src/components/SettingsModal.tsx`
- `src/index.css`
- `src/App.tsx`

## Verify (run these before calling the card done)

- [x] Mở app trên trình duyệt: Bottom Dock hiển thị nền trắng kính mờ đẹp mắt, icon sắc nét và hài hòa với theme sáng.
- [x] Mở modal Cài đặt ⚙️: Hiển thị đầy đủ cả Mã PIN và Mật khẩu phòng; bấm Copy mã PIN và Copy Mật khẩu sao chép thành công vào clipboard; không còn ô nhập Gemini API Key.
- [x] `npm run build` chạy thành công không có lỗi TypeScript / Tailwind.

## Done-evidence (world-state proof, named BEFORE building)

Kiểm tra trực quan Bottom Dock màu trắng và modal Cài đặt hiển thị Passcode trên trình duyệt, output `npm run build`.

## Evidence (paste the actual proof here when done)

```bash
$ npm run build
vite v6.4.3 building for production...
✓ 1844 modules transformed.
dist/index.html                   0.98 kB │ gzip:  0.55 kB
dist/assets/index-BUzXhC56.css   30.55 kB │ gzip:  5.91 kB
dist/assets/index-ChP5tKRK.js   208.75 kB │ gzip: 61.52 kB
✓ built in 1.08s
PWA v1.3.0
files generated: dist/sw.js, dist/workbox-9c191d2f.js

Bottom Dock đã chuyển sang màu trắng Frosted Glassmorphism.
Modal Cài đặt hiển thị Mã PIN + Mật khẩu phòng + Nút Sao chép 1 chạm; đã xóa bỏ hoàn toàn Form nhập Gemini Key thừa.
```


