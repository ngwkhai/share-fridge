# C-018 — Enforce room authorization and prevent room takeover

status: todo
deps: C-017

## Scope

F01/F02: enforce Bearer room sessions on foods, shopping, AI, notifications and room detail; verify ownership for ID routes; whitelist public room DTOs; reject duplicate codes on both create endpoints; validate PIN/passcode; production must require a strong SESSION_SECRET; reject expired or malformed tokens. Add regression tests for unauthenticated and cross-room operations. Retain existing data rather than resetting it.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `server/*`
- `api/*`
- `tests/*`
- `package.json`
- `package-lock.json`
- `cards/C-018.md` (evidence only)
- `evidence/C-018/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [ ] npm test passes with authenticated fixtures, including 401 without token, 403/404 for another room, duplicate create 409 and no hash/salt in responses.
- [ ] npm run build passes.
- [ ] On the linked deployment, create two disposable rooms and prove unauthorized read/write and overwrite are rejected; paste sanitized HTTP results and /api/openapi.json URL.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-018/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

(empty until done)
