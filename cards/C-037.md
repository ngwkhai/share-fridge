# C-037 — Sao lưu đã thử khôi phục thật, và runbook sự cố

status: todo
deps: C-034

## Scope

Một bản sao lưu chưa từng được restore không phải là bản sao lưu. C-028 đã tạo được một phần runbook
rollback; card này mở rộng nó và chứng minh đường khôi phục dữ liệu thực sự hoạt động.

- Xác nhận sao lưu hàng ngày của Supabase Pro đang chạy; ghi rõ RPO/RTO chấp nhận được
  (đề xuất: mất tối đa 24 giờ dữ liệu, khôi phục trong 4 giờ).
- THỰC HIỆN một lần khôi phục thật vào một project rỗng và đo thời gian thật.
- Mở rộng runbook với các kịch bản đã có tiền lệ hoặc rủi ro cao: mất DB, hết hạn/lộ khóa VAPID,
  Google ngừng model Gemini (đã xảy ra một lần với `gemini-2.5-flash`), tên miền hết hạn,
  và khóa upload của Play bị mất.
- Runbook phải viết cho người khác đọc, không dựa vào trí nhớ của operator.

P5: khôi phục phải vào project RỖNG, tuyệt đối không restore đè lên production.

## Allowed files

- `DEPLOY_GUIDE.md`
- `evidence/C-037/`
- `AUTO-LOG.md`

## Verify (run these before calling the card done)

- [ ] Supabase dashboard cho thấy sao lưu hàng ngày đang bật, có ít nhất 2 bản sao lưu liên tiếp.
- [ ] Thực hiện restore thật vào project rỗng ⇒ truy vấn được dữ liệu, `SELECT count(*) FROM foods` trả đúng số dòng của bản sao lưu; ghi lại số phút thực tế từ lúc bắt đầu tới lúc truy vấn được.
- [ ] Số phút đo được nằm trong RTO đã cam kết; nếu vượt, sửa RTO cho đúng sự thật thay vì làm ngơ.
- [ ] Xóa project khôi phục sau khi đo xong; xác nhận production không bị đụng tới trong toàn bộ quá trình.
- [ ] Một người khác (hoặc chính operator sau 1 tuần) đọc runbook và thực hiện được bước rollback mà không hỏi thêm.

## Done-evidence (world-state proof, named BEFORE building)

- Log/ảnh chụp lần khôi phục thật kèm số phút thực tế và output `SELECT count(*)`.
- `DEPLOY_GUIDE.md` có mục runbook đủ 5 kịch bản.
- Xác nhận project khôi phục đã được xóa.

## Evidence (paste the actual proof here when done)

(empty until done)
