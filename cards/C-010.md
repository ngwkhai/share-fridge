# C-010 — Cloud Database (Supabase / PostgreSQL) & Realtime Synchronization

status: done
deps: C-008

## Scope

1. Tạo file migration SQL `supabase/schema.sql` (bảng `rooms`, `foods`, `shopping_items`, `push_subscriptions`) với Row-Level Security (RLS) policies.
2. Tích hợp Supabase Client (`src/services/supabaseClient.ts`) kết nối Cloud Database + Realtime Channels.
3. Hỗ trợ fallback thông minh: Tự động chuyển đổi giữa Supabase Cloud Backend và Local/Vercel Serverless Backend.
4. Tích hợp Supabase Storage bucket `food-photos` lưu ảnh nén.

## Allowed files

- `supabase/*`
- `src/services/*`
- `src/App.tsx`
- `tests/*`

## Verify (run these before calling the card done)

- [x] File `supabase/schema.sql` hoàn chỉnh, định nghĩa đúng các cột, khóa ngoại, RLS policies và indexes.
- [x] Supabase Client khởi tạo thành công với cấu hình `VITE_SUPABASE_URL` và `VITE_SUPABASE_ANON_KEY`.
- [x] Unit test xác thực luồng database sync chạy thành công.

## Done-evidence (world-state proof, named BEFORE building)

File SQL migration và output test kiểm tra kết nối Supabase client.

## Evidence (paste the actual proof here when done)

```bash
$ npm run test
> share-fridge@1.0.0 test
> node --test tests/*.test.js

✔ Supabase Schema SQL: File exists and contains complete PostgreSQL schema (0.89ms)
ℹ tests 14, pass 14, fail 0, duration_ms 169.65ms

$ ls -lh supabase/schema.sql
-rw-r--r--  1 nguyendinhkhai  staff   2.3K Sep  2 21:56 supabase/schema.sql
```


