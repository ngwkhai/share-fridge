# C-029 — Hợp nhất nhánh phát hành về main và biến CI thành cổng thật

status: done
deps: none

## Scope

`main` đang ở `eb4c320` ("Baseline: capture audited project and remediation flow"). Toàn bộ 11 card
khắc phục C-018 → C-028 nằm trên `codex/C-021-delta-fast-path`, cách main 51 commit và 0 commit phía
sau (main là tổ tiên — merge sẽ là fast-forward). Vì `.github/workflows/ci.yml` chỉ chạy trên `main`
và PR nhắm vào `main`, CI CHƯA TỪNG bảo vệ đoạn code đang phục vụ production. Việc này vi phạm quy
tắc "main = deployable" của chính CLAUDE.md và chặn mọi quy trình phát hành sau đó (versionCode đơn
điệu, rollback, hotfix).

Card này chỉ làm MỘT việc: đưa `main` trở lại đúng vai trò nhánh phát hành.

- Merge fast-forward `codex/C-021-delta-fast-path` → `main`.
- Gắn tag `v1.0.0` tại commit đang phục vụ production.
- Trỏ Production Branch của Vercel về `main` (thao tác dashboard/CLI — cần operator nếu thiếu quyền).
- Xóa các nhánh `codex/*` đã được merge hoàn toàn vào main; giữ nguyên nhánh nào còn commit chưa merge.
- Thêm bước sinh `versionCode` đơn điệu vào CI (đếm commit trên main), chuẩn bị sẵn cho C-043.

KHÔNG thuộc phạm vi: sửa `vercel.json` cho `.well-known` (đó là C-042), nâng bậc dịch vụ (C-034),
hay bất kỳ thay đổi mã sản phẩm nào.

### Sửa đổi phạm vi khi đang xây (2026-09-05, ghi trước khi tiếp tục)

Phát hiện trong lúc thực hiện: **repo không có git remote nào** (`git remote -v` trống, không tồn tại
`.git/refs/remotes`). Nghĩa là `.github/workflows/ci.yml` do C-027 tạo ra CHƯA TỪNG chạy một lần nào
và không thể chạy — GitHub Actions chỉ tồn tại khi repo nằm trên GitHub. C-027 ghi "CI workflow created"
là đúng theo nghĩa đen (một file đã được tạo), nhưng world-state "CI là cổng" chưa bao giờ đúng.

Hệ quả cho card này: mục Verify "mở PR thử nghiệm ⇒ CI báo đỏ" KHÔNG thực hiện được trong session này.
Nó trở thành hành động của operator (tạo repo GitHub, push, bật branch protection cho main). Workflow
được viết sẵn để trở thành cổng thật ngay khi có remote, và được đánh dấu rõ trong chính file đó.

Hệ quả thứ hai, phát hiện khi chạy `npm run build`: `node_modules` trong thư mục này chứa binary
**macOS** (`@rollup/rollup-darwin-arm64`, `@esbuild/darwin-arm64`) trong khi shell chạy trên
**Linux aarch64**. Bước đóng gói vite vì thế không chạy được từ đây — đây là hiện vật của môi trường,
KHÔNG phải lỗi của main. Không cài đè `node_modules` để tránh phá môi trường dev trên máy operator.
Phần độc lập nền tảng (`tsc --noEmit`) đã chạy và sạch.

## Allowed files

- `.github/workflows/ci.yml`
- `AUTO-LOG.md`
- `DEPLOY_GUIDE.md` (bổ sung 2026-09-05: quy trình dựng remote GitHub thuộc về card này vì chính nó là điều kiện để CI trở thành cổng thật)
- `playbooks/github-release-gate.md` + dòng index trong `playbooks/README.md` (bổ sung 2026-09-06: card này trả giá cho ba bài học stack không hiển nhiên — `secrets` trong `if:`, branch protection theo gói, và tag phát hành phải đọc từ `meta.gitCommitSha`. CLAUDE.md quy định card chưa xong khi bài học chưa được nộp vào playbooks/)
- thao tác git trên nhánh/tag (không phải file)
- cấu hình Vercel Production Branch (ngoài repo)

## Verify (run these before calling the card done)

- [x] `git log --oneline main -1` trả về đúng SHA của bản dựng đang phục vụ https://sharefridge.vercel.app (đối chiếu bằng `vercel inspect` hoặc dashboard), KHÔNG còn là `eb4c320`.
- [x] `git log --oneline HEAD..main | wc -l` và `git log --oneline main..HEAD | wc -l` đều trả 0 khi đứng ở nhánh tích hợp — chứng minh merge đã trọn vẹn, không sót commit.
- [x] (sửa 2026-09-06) `git rev-list -n1 v1.0.0` bằng đúng `meta.gitCommitSha` của deployment production,
  và `git merge-base --is-ancestor v1.0.0 main` trả 0. Câu hỏi gốc `git tag --points-at main` chỉ đúng khi
  đỉnh main TRÙNG commit phát hành; main đã đi tiếp (quy hoạch v7 + sửa CI) nên phép thử đó sai từ trong
  thiết kế — một tag phát hành phải nằm TRONG lịch sử main, không phải ở đỉnh main.
- [x] Nối repo GitHub vào dự án Vercel TRƯỚC (Settings -> Git -> Connect), vì hiện dự án chưa nối Git nên thiết lập Production Branch chưa tồn tại. Sau khi nối: Production Branch = `main`, một deploy mới sinh ra từ commit trên `main`, và `https://sharefridge.vercel.app/healthz` vẫn trả 200.
- [x] Mở một PR thử nghiệm cố tình làm hỏng một unit test ⇒ CI chạy và BÁO ĐỎ (chứng minh CI thật sự là cổng, không phải trang trí); đóng PR sau khi chụp bằng chứng.
- [x] `git branch --merged main | grep codex` — mọi nhánh còn lại trong danh sách này đã được xóa; `git branch --no-merged main` không mất nhánh nào có việc chưa merge.

## Done-evidence (world-state proof, named BEFORE building)

- Output thật của `git log --oneline main -1` và `git tag --points-at main`.
- Ảnh chụp hoặc output CLI cho thấy Vercel Production Branch = `main`.
- URL của deployment production mới sinh ra từ `main`, kèm phản hồi 200 của `/healthz` với `version` và `timestamp` thật.
- URL/ảnh của lần chạy CI báo đỏ trên PR thử nghiệm.

## Evidence (paste the actual proof here when done)

**PARTIAL — 2026-09-05.** Phần git đã hoàn tất và kiểm chứng được; phần Vercel và CI chặn trên
operator (xem "Sửa đổi phạm vi" ở trên). Card giữ `status: todo` theo AUTO PRINCIPLE P1.

### Đã chứng minh

Trước khi merge:
```
main        = eb4c3207a8a4316e24eb56606f5f93b4d6458a5d  Baseline: capture audited project and remediation flow
integration = 8a002c62c829b2d99d5d4b4c80c74741bf225e62
main..integration = 52 commits
```

Fast-forward đã xác minh an toàn trước khi thực hiện (`git merge-base --is-ancestor main HEAD` ⇒ true,
`HEAD..main` = 0 commit), sau đó `git merge --ff-only`. Sau khi merge:
```
$ git log -1 --format='%H %ci %s' main
8a002c62c829b2d99d5d4b4c80c74741bf225e62 2026-09-05 11:38:56 +0000 v7 planning: store-release ADR, scope, PRD, contract v3 and cards C-029..C-046

$ git rev-list --count main..HEAD ; git rev-list --count HEAD..main
0
0

$ git tag -l -n1
archive/main-pre-v7  Baseline: capture audited project and remediation flow
v1.0.0               v1.0.0 - remediation release C-018..C-028, verified live on production 2026-09-04 (deployment dpl_Apfbe52eAZuEBoNqyF1pU3ANAyRV per AUTO-LOG)

$ git branch --no-merged main
(none - moi nhanh deu da nam tron trong main)

$ git branch -a
* main
```
Mười nhánh `codex/*` đã xóa sau khi xác nhận cả mười đều nằm trọn trong main. Tag
`archive/main-pre-v7` giữ lại con trỏ baseline cũ để hoàn tác được.

main thật sự deployable ở phần kiểm chứng được từ đây:
```
$ npm test
# tests 118 / # pass 118 / # fail 0

$ npx tsc --noEmit ; echo $?
0
```

### Phát hiện đáng kể trong lúc xây: bộ chống trôi hợp đồng đã bắt lỗi của chính tôi

Lần chạy `npm test` đầu tiên sau khi sửa quy hoạch cho ra **117 pass / 1 fail**. Test đỏ là
`tests/client_contract.test.js:178` — "every planning method/path exists in runtime spec", báo
`delete /api/rooms/{code}`. Đây KHÔNG phải hồi quy: test này đọc mọi dòng bảng
`| METHOD | \`path\` |` trong `flow/05-contract.md` và bắt buộc route đó phải có trong
`/api/openapi.json` đang chạy. Tôi đã đưa 6 route v3 vào bảng endpoint trước khi chúng được xây,
nên bộ chống trôi báo đúng.

Sửa theo đúng quy tắc "spec lands WITH the API" của CLAUDE.md: bảng endpoint là tập route PHẢI có
thật trong spec đang chạy, nên route mới được thiết kế nhưng chưa xây không thuộc về bảng đó. Sáu
route đã được chuyển sang mục "Planned routes" với cột `Lands with` chỉ rõ card nào sẽ đưa nó vào
bảng cùng lúc với code. Sau khi sửa: **118/118 pass**.

### Chưa chứng minh (chặn trên operator)

- SHA của main khớp bản dựng đang phục vụ production: shell trên máy operator KHÔNG có mạng
  (`curl` trả 000) và không có Vercel CLI, nên không tự xác minh được. Việc gắn `v1.0.0` tại
  `35a8355` dựa trên bản ghi AUTO-LOG về deployment `dpl_Apfbe52eAZuEBoNqyF1pU3ANAyRV`, KHÔNG phải
  bằng chứng độc lập của session này.
- Vercel Production Branch = `main`: cần thao tác của operator.
- CI báo đỏ trên PR thử nghiệm: chặn cứng vì repo chưa có remote.

### Sửa thứ tự Verify (2026-09-05, phát hiện khi tìm cách tự thực hiện)

Operator cho phép Claude tự làm hai việc treo. Đã kiểm tra hết đường và kết luận là KHÔNG làm được,
không phải vì thiếu quyền mà vì thiếu credential và vì egress:

- Shell của Claude trên máy operator là một VM Linux tách biệt (`Linux claude 6.8.0 aarch64`,
  HOME `/sessions/...`) chỉ mount đúng thư mục ShareFridge. `/Users`, `~/.ssh`, `~/.config/gh`,
  config Vercel CLI đều không tồn tại trong đó. Việc operator từng push được từ Terminal là đúng —
  nhưng đó là Terminal của máy Mac, không phải shell này.
- `GH_TOKEN` trong container đám mây là placeholder rỗng ("builtin injection failed").
- `github.com` tới được qua HTTPS từ VM nhưng không có credential; SSH không phân giải được DNS.
- **`api.vercel.com` bị chặn từ CẢ HAI môi trường Claude chạy được** — kể cả có token Vercel cũng
  không gọi được.
- Operator đã từ chối bật computer use, nên không có đường điều khiển máy Mac.

Phát hiện kèm theo, quan trọng hơn: dự án Vercel **chưa nối với Git repo nào**
(`.vercel/project.json` chỉ có projectId/orgId/projectName; AUTO-LOG ghi mọi lần lên production đều
bằng `vercel deploy` từ CLI). Nghĩa là "Production Branch = main" chưa phải một thiết lập tồn tại —
nó chỉ xuất hiện sau khi nối repo GitHub vào dự án. Hai mục treo vì thế NỐI TIẾP nhau, không song
song, và cả hai đều phụ thuộc vào việc repo GitHub ra đời trước. Mục Verify đã được sửa cho đúng
thực tế này. Quy trình ba bước copy-paste đã ghi trong `DEPLOY_GUIDE.md`.

### Kiểm tra bảo mật trước khi push (2026-09-05)

Operator chọn tạo GitHub private repo. Không có `gh` CLI và không có credential GitHub ở cả hai
phía, nên việc tạo repo là thao tác của operator. Trước khi giao lại, đã quét bí mật — đẩy nhầm
một khóa lên GitHub là không thu hồi được:

```
$ git ls-files | grep -iE '\.env($|\.)'
.env.example          # chỉ file mẫu, đúng như .gitignore quy định

$ git grep -I -nE 'AIza[0-9A-Za-z_-]{30,}|sb_secret_[A-Za-z0-9_-]{10,}|BEGIN [A-Z ]*PRIVATE KEY|postgres(ql)?://[a-zA-Z0-9._-]+:[^ "@]{8,}@[a-z0-9.-]*(supabase|neon|amazonaws|rds)' $(git rev-list --all)
(không có kết quả — 54 commit)
```

Ba kết quả khớp trong file hiện tại đã được đọc từng dòng và đều lành: placeholder
`username:password@database-host` trong `.env.example`, một câu văn mô tả service_role key trong
`DEPLOY_GUIDE.md`, và fixture tổng hợp trong `tests/realtime_security.test.js`
(`'x'.repeat(48)`, `sb_publishable_fixture`). Kết luận: **an toàn để push**. Kích thước pack 26,91 MiB.

---

## Cập nhật 2026-09-06 — session chạy TRÊN máy Mac, năm việc treo đã đóng

Session trước chạy trong VM Linux tách biệt nên không với tới credential. Session này chạy
trực tiếp trên máy Mac (`Darwin Khai-Nguyen-Dinh-MacBook.local 24.6.0 arm64`), có `gh` đã
đăng nhập (`ngwkhai`), có mạng, và refresh được token Vercel CLI. Bốn trong năm việc treo
đã đóng bằng bằng chứng thật.

### 1. Remote GitHub đã tồn tại và main đã lên

```
$ gh repo create share-fridge --private --source=. --remote=origin --push
https://github.com/ngwkhai/share-fridge
 * [new branch]      HEAD -> main

$ git push origin --tags
 * [new tag]         archive/main-pre-v7 -> archive/main-pre-v7
 * [new tag]         v1.0.0 -> v1.0.0

$ git ls-remote --heads --tags origin
b1ec899a4b25a2eb05dd1125ec4251682e610cdf  refs/heads/main
eb4c3207a8a4316e24eb56606f5f93b4d6458a5d  refs/tags/archive/main-pre-v7
```

Quét bí mật chạy lại trên toàn bộ **56 commit** trước khi push: không kết quả. Quét lần hai
trên file đang theo dõi (thêm mẫu `gho_`/`ghp_`/`github_pat_`/JWT `eyJ...`/`sk-`/
`vercel_blob_rw_`) trước khi chuyển public: chỉ khớp đúng một dòng — là chính câu lệnh quét
được chép trong card này. File `evidence/**/*.log` đã được sanitize từ các card trước.

### 2. CI thật sự chạy — và lần chạy đầu tiên phơi ra một lỗi vô hình suốt hai card

Push đầu tiên tạo run [33981069466](https://github.com/ngwkhai/share-fridge/actions/runs/33981069466):
`conclusion: failure`, **`total_count: 0` job**, không annotation nào, chỉ dòng "This run
likely failed because of a workflow file issue".

Nguyên nhân: hai bước Playwright guard bằng `${{ vars.BASE_URL != '' || secrets.BASE_URL != '' }}`.
Context `secrets` KHÔNG dùng được trong `if:` — dùng ở đó làm invalid cả file workflow, nên
GitHub không lên lịch job nào. Lỗi này không thể phát hiện được khi repo chưa có remote: C-027
viết file, C-029 (session trước) sửa file, cả hai đều không có cách nào chạy nó.

Sửa trong PR [#1](https://github.com/ngwkhai/share-fridge/pull/1) → CI xanh:
```
✓ card/C-029 ci.yml ngwkhai/share-fridge#1 · 33981187108
✓ build-and-test in 43s
  ✓ Compute versionCode   ✓ Install dependencies   ✓ Build project
  ✓ Runtime spec must match its source              ✓ Run unit and contract tests
  - Install Playwright Browsers (skipped: BASE_URL chưa đặt)
```
Merged squash → main = `b4e84e7`.

### 3. CI là CỔNG THẬT — đã chứng minh bằng PR cố tình làm đỏ

PR [#2](https://github.com/ngwkhai/share-fridge/pull/2) thêm một assert `assert.equal(1, 2)`
vào `tests/client_contract.test.js`. Run [33981319977](https://github.com/ngwkhai/share-fridge/actions/runs/33981319977):

```
✓ Build project
✓ Runtime spec must match its source
X Run unit and contract tests        <-- đỏ đúng chỗ
- Install Playwright Browsers
X Process completed with exit code 1

not ok 4 - tests/client_contract.test.js
# tests 107 / # pass 106 / # fail 1
```
PR đã đóng không merge, nhánh `ci-gate-proof` đã xóa cả local lẫn remote. `git branch -a` chỉ
còn `main`; `gh api repos/ngwkhai/share-fridge/branches` chỉ trả `main`.

### 4. Branch protection — free plan chặn, operator chọn chuyển public

`PUT /branches/main/protection` và Rulesets đều trả:
```
{"message":"Upgrade to GitHub Pro or make this repository public to enable this feature.","status":"403"}
```
Operator chọn **chuyển repo sang public**. Sau đó bật được ngay:
```
$ gh api repos/ngwkhai/share-fridge --jq '{visibility, private}'
{"private":false,"visibility":"public"}

$ gh api -X PUT repos/ngwkhai/share-fridge/branches/main/protection ...
required_status_checks: ['build-and-test'] strict= True
enforce_admins: True
required_pull_request_reviews: 0
allow_force_pushes: False allow_deletions: False
```

Cổng được chứng minh là CỨNG, không phải chỉ báo cáo — thử push thẳng vào main (bằng chính
tài khoản owner, `enforce_admins: true` nên admin cũng không lách được):
```
$ git push origin card/C-029-remote:main
remote: error: GH006: Protected branch update failed for refs/heads/main.
remote: - Changes must be made through a pull request.
remote: - Required status check "build-and-test" is expected.
 ! [remote rejected] card/C-029-remote -> main (protected branch hook declined)
```

### 5. Tag v1.0.0 đã trỏ SAI — đã sửa

Đây là phát hiện đáng giá nhất của session. Vercel API cho biết deployment production đang
chạy build từ commit nào:
```
$ curl -H "Authorization: Bearer $VT" \
    "https://api.vercel.com/v13/deployments/dpl_Apfbe52eAZuEBoNqyF1pU3ANAyRV?teamId=team_NJz..."
sha    = 2ad793ed2724f718e179ae13922b8a12924577c0
ref    = codex/C-021-delta-fast-path
target = production READY
```
Session trước gắn `v1.0.0` tại `35a8355` bằng SUY LUẬN từ AUTO-LOG (không có mạng để kiểm
chứng) — lệch một commit. Phần chênh lệch chỉ là file bằng chứng C-028, không có mã sản phẩm:
```
$ git diff --stat 2ad793e 35a8355 -- src/ api/ server/ public/ package.json \
    package-lock.json vite.config.ts vercel.json index.html tsconfig.json
(rỗng)
```
Operator cho phép dời tag. Sau khi dời:
```
$ git rev-list -n1 v1.0.0
2ad793ed2724f718e179ae13922b8a12924577c0
$ git merge-base --is-ancestor v1.0.0 main ; echo $?
0
$ git ls-remote --tags origin v1.0.0
ab0ef6ba5107e5396c4057fa49bd4fe0335339bc  refs/tags/v1.0.0   (annotated -> 2ad793e)
```
`git checkout v1.0.0` giờ tái tạo đúng bản đang chạy production — điều kiện cần cho rollback.

### 6. `npm run build` — lần đầu tiên chạy thật, trên máy Mac

Việc treo từ session trước (node_modules là binary macOS, shell Linux không chạy được):
```
$ npm run build
vite v6.4.3 building for production...
✓ 1895 modules transformed.
dist/assets/index-C0AMoErZ.css   35.67 kB │ gzip:   6.66 kB
dist/assets/index-DcoO8ngS.js   482.32 kB │ gzip: 135.14 kB
✓ built in 1.39s
PWA v1.3.0  precache 8 entries (507.88 KiB)

$ npx tsc --noEmit ; echo $?
0
$ node server/build-openapi.js --stdout > /tmp/generated.json
$ diff <(jq -S . /tmp/generated.json) <(jq -S . server/openapi.json)   # rỗng: spec khớp nguồn
$ npm test
# tests 118 / # pass 118 / # fail 0
```

### Nối Vercel với repo — ĐÃ XONG 2026-09-06 (nguyên nhân KHÁC với giả định ban đầu)

`vercel git connect` trả 400 "You need to add a Login Connection to your GitHub account first".
Mở dashboard mới thấy nguyên nhân thật: **có HAI tài khoản Vercel.**

| | Tài khoản | GitHub connection | Thấy dự án? |
|---|---|---|---|
| Vercel CLI | `khaindhrt-9606` (khaind.hrt@gmail.com) | chưa có | Có — OWNER của team sở hữu `sharefridge` |
| Trình duyệt | `nguyendinhkhaiqt2005-8486` (nguyendinhkhaiqt2005@gmail.com) | đã nối `ngwkhai` | Không — URL dự án trả 404 |

```
$ curl -H "Authorization: Bearer $VT" https://api.vercel.com/v2/teams/team_NJz.../members
 - khaindhrt-9606  khaind.hrt@gmail.com  role=OWNER  confirmed=True     (chỉ một thành viên)
```
GitHub `ngwkhai` đang bị gắn vào tài khoản Vercel KHÔNG sở hữu dự án. Đây là lý do phần này
tạm dừng: bước tiếp theo là **đăng nhập** vercel.com bằng khaind.hrt@gmail.com, việc Claude
không được phép làm thay.

> **Cập nhật ở mục 7 bên dưới:** operator đã đăng nhập và việc nối đã xong. Chẩn đoán ở trên
> đúng phần "sai tài khoản", nhưng SAI phần "phải tháo `ngwkhai` khỏi tài khoản kia trước" —
> thực tế không cần tháo gì. Giữ nguyên đoạn này để thấy suy luận nào đã sai ở đâu.

Trạng thái live tại thời điểm ghi:
```
$ curl -s https://sharefridge.vercel.app/healthz
{"status":"ok","version":"1.0.0","timestamp":"2026-09-05T17:37:58.998Z"}
```

### 7. Nối Vercel — đã xong sau khi operator đăng nhập đúng tài khoản

Operator đăng nhập vercel.com bằng `khaind.hrt@gmail.com`. Trang Authentication khi đó cho thấy
dòng GitHub vẫn ở trạng thái **"Connect your GitHub account"** — tức đăng nhập thôi chưa đủ, còn
thiếu đúng một cú bấm. Operator ủy quyền cho Claude bấm nút đó và chạy luồng OAuth trong Chrome.

Sau khi nối, dòng GitHub hiển thị `ngwkhai` / "Last used just now" trên tài khoản `khaindhrt-9606`.
Lo ngại "một danh tính GitHub chỉ gắn được một tài khoản Vercel" hóa ra KHÔNG chặn ở đây — không
cần tháo khỏi tài khoản `nguyendinhkhaiqt2005-8486` trước.

```
$ npx vercel git connect https://github.com/ngwkhai/share-fridge --yes
> Found a repository in your local Git Config: https://github.com/ngwkhai/share-fridge.git
> Connecting GitHub repository: https://github.com/ngwkhai/share-fridge
> Connected
```

Kiểm chứng qua API — `link` không còn `null`, và `productionBranch` đã là `main` ngay từ đầu:
```
$ curl -H "Authorization: Bearer $VT" \
    "https://api.vercel.com/v9/projects/prj_jU4kMzQFFQnTIcN4yVHKxq79LL4i?teamId=team_NJz..."
{
 "name": "sharefridge",
 "link": {
  "type": "github", "org": "ngwkhai", "repo": "share-fridge",
  "repoId": 1358412718,
  "productionBranch": "main",
  "gitCredentialId": "cred_e6954651f5ee7d5995147da4f867aa86d83cf0fc"
 },
 "productionBranch": "main"
}
```

Kiểm tra trước khi để lần deploy-từ-Git ĐẦU TIÊN chạy (nếu build hỏng, Vercel giữ nguyên bản
production đang chạy — nhưng vẫn phải soi trước):
```
framework   = vite        buildCommand = npm run build
outputDirectory = dist    nodeVersion  = 24.x        rootDirectory = None
```
Khớp đúng bộ lệnh đã chạy xanh trên máy Mac. 14 biến môi trường production đều có mặt:
`DATABASE_URL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET`,
`SUPABASE_ANON_KEY`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SESSION_SECRET`,
`GOOGLE_CLIENT_ID`, `GEMINI_API_KEY`, `CRON_SECRET`, `VAPID_SUBJECT`, `VAPID_PUBLIC_KEY`,
`VAPID_PRIVATE_KEY`.

### 8. Deploy production ĐẦU TIÊN sinh từ Git — mục Verify cuối cùng đã đóng

Merge PR #4 (operator tự chạy `gh pr merge`, vì merge lúc này đã đồng nghĩa với phát hành)
sinh ra deployment production đầu tiên trong lịch sử dự án có nguồn là một commit trên `main`:

```
uid       = dpl_EyNWphzhSDN5wSuyzwTeENX3gWzq
state     = READY | target = production | source = git      <-- trước đây luôn là "cli"
sha       = 29c13f35403aee3d7827662f0aff441f6ddb534e
ref       = main
repo      = ngwkhai / share-fridge
created   = 2026-09-06 07:42:15

$ git rev-parse main
29c13f35403aee3d7827662f0aff441f6ddb534e                    <-- khớp tuyệt đối
```

Domain production thật sự trỏ vào bản này (không chỉ là bản build thành công nằm im):
```
$ curl -H "Authorization: Bearer $VT" ".../v2/deployments/dpl_EyNWph.../aliases?teamId=..."
 - sharefridge.vercel.app
 - sharefridge-git-main-khaindhrt-9606s-projects.vercel.app
 - sharefridge-khaindhrt-9606s-projects.vercel.app
```

Kiểm chứng trên URL công khai sau khi deploy:
```
$ curl https://sharefridge.vercel.app/healthz
{"status":"ok","version":"1.0.0","timestamp":"2026-09-06T00:43:17.830Z"}          HTTP 200
$ curl https://sharefridge.vercel.app/readyz
{"status":"ok","database":"postgres"}                                             HTTP 200
$ curl https://sharefridge.vercel.app/api/openapi.json | ...
paths: 25 | version: 2.0.0
$ curl -o /dev/null -w '%{http_code}' https://sharefridge.vercel.app/
200
```

Rủi ro của lần chuyển đổi này bằng 0 và điều đó kiểm chứng được — mã sản phẩm giữa `v1.0.0`
và commit đang chạy không khác một dòng:
```
$ git diff --stat v1.0.0 29c13f3 -- src/ api/ server/ public/ package.json \
    package-lock.json vite.config.ts vercel.json index.html tsconfig.json
(rỗng)
```
Toàn bộ 7 commit từ `v1.0.0` đến production hiện tại là quy hoạch v7, tài liệu và hạ tầng CI.
Nói cách khác: hệ thống phát hành đã thay đổi hoàn toàn, sản phẩm thì không — đúng như một
card hạ tầng phải làm.

### Kết luận C-029

`main` giờ đúng là nhánh phát hành: được CI bảo vệ cứng (`enforce_admins`, push thẳng bị
`GH006` từ chối), mỗi commit vào nó sinh một bản production gắn với đúng SHA đó, và `v1.0.0`
trỏ vào commit tái tạo được bản đang chạy. Vòng luẩn quẩn "production đến từ CLI trên máy cá
nhân, main thì tụt lại 52 commit" đã đóng. `status: done`.

**Hệ quả phải nhớ:** từ đây merge một PR LÀ phát hành production. Đừng dùng `vercel deploy`
từ CLI song song nữa — nó tạo bản production không gắn với commit nào trên main.
