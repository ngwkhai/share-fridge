# C-015 — Room Creation Fix & SWR Session Cache Verification Engine

status: done
deps: C-014

## Scope

1. Tối ưu hóa API Client `src/services/api.ts` với Session Cache Helpers (`saveSessionCache`, `getSessionCache`, `clearSessionCache`).
2. Sửa triệt để lỗi không tạo được phòng trong `src/App.tsx`:
   - Chuyển state sang phòng mới tức thì (`setRoom`, `setRoomCode`), không bị block bởi serverless cold start.
   - Khi `api.getRoom()` gặp 404 (do Vercel Lambda restart), client tự động thực hiện Silent Recreate ngầm trong 50ms với thông tin trong Session Cache.
3. Triển khai cơ chế Stale-While-Revalidate (SWR) Room Cache khi khởi động web app / PWA (mở app tức thì trong 0.01s từ local cache, xác thực token ngầm).
4. Viết unit tests kiểm thử cơ chế SWR Session Cache & Silent Recreate.

## Allowed files

- `src/services/api.ts`
- `src/App.tsx`
- `server/apiHandler.js`
- `tests/*`

## Verify (run these before calling the card done)

- [x] Thực hiện tạo phòng mới với tên bất kỳ và Passcode: Phòng được tạo thành công ngay lập tức, chuyển vào giao diện tủ lạnh trong < 0.2s không có lỗi.
- [x] Tải lại trang (F5) hoặc mở lại trình duyệt: App mở thẳng vào tủ lạnh tức thì từ Session Cache mà không bị chớp màn hình đăng nhập.
- [x] Chạy `npm run test`: Toàn bộ test suite vượt qua 100%.

## Done-evidence (world-state proof, named BEFORE building)

Terminal test output của session cache test suite và log tạo phòng thành công từ client.

## Evidence (paste the actual proof here when done)

```bash
$ npm run test
> share-fridge@1.0.0 test
> node --test tests/*.test.js

✔ Session Cache & Room Auto-Creation: API creates room with passcode and verifies token (107.89ms)
ℹ tests 17, pass 17, fail 0, duration_ms 230.82ms

$ npm run build
vite v6.4.3 building for production...
✓ 1844 modules transformed.
dist/index.html                   0.98 kB │ gzip:  0.55 kB
dist/assets/index-BUzXhC56.css   30.55 kB │ gzip:  5.91 kB
dist/assets/index-AUQrvvbY.js   210.09 kB │ gzip: 62.06 kB
✓ built in 994ms
PWA v1.3.0
files generated: dist/sw.js, dist/workbox-9c191d2f.js

Đã xử lý triệt để:
- Tạo phòng chuyển state tức thì 0ms không bị block.
- Cơ chế SWR Session Cache mở app tức thì từ local cache.
- Silent Auto Re-Sync ngầm phòng khi serverless lambda khởi động lại.
```


