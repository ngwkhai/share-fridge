# C-024 — Deliver authenticated scheduled Web Push notifications

status: todo
deps: C-017

## Scope

F06: real PushManager subscription with public VAPID key, persistent device subscriptions, authenticated subscribe/unsubscribe/config routes, actual sender and expired-endpoint cleanup. Integrate push handler into active PWA worker. Schedule expiry reminder at 16:30 Asia/Ho_Chi_Minh with deduplication; add roommate-change notifications without notifying the acting device where identifiable. Gate scheduled dispatch with CRON_SECRET and avoid success UI on failure.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `server/*`
- `api/*`
- `supabase/*`
- `src/components/NotificationModal.tsx`
- `src/App.tsx` (device subscription and logout/room lifecycle only)
- `src/services/*`
- `src/types/*`
- `public/sw-push.js`
- `vite.config.ts`
- `vercel.json`
- `tests/*`
- `scripts/*`
- `package.json`
- `package-lock.json`
- `.env.example`
- `DEPLOY_GUIDE.md`
- `cards/C-024.md` (evidence only)
- `evidence/C-024/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [x] Test real subscription shapes, authorization, sender adapter failures, 404/410 cleanup, correct local date/time and duplicate cron runs. Confirmed live against a real Supabase project and a real provider 410 response, not just mocked.
- [ ] Build service worker and verify push/notificationclick handlers execute; rejected subscribe never reports enabled. (Build + embedding verified; handler execution verified only in a simulated worker global, not a real browser — see evidence.)
- [ ] Receive an actual notification on a supported locked/closed-app device from the deployed scheduler; record timestamp, timezone and dedup result. Blocked: Vercel Cron only runs on Production, and this Preview has Deployment Protection (SSO) that would also block a phone browser — needs an operator decision to promote/expose before a physical device can be tested.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-024/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

PARTIAL — implemented, wired end-to-end and verified locally; status remains `todo`.

- [Evidence summary](../evidence/C-024/README.md): what was verified, what wasn't, and why.
- [Unit/contract tests: 89 passed, 0 failed](../evidence/C-024/unit-tests.log)
- [PostgreSQL integration: 11 passed, 0 failed](../evidence/C-024/postgres.log)
- [Production build: exit 0](../evidence/C-024/build.log)
- [`dist/sw.js` embeds `importScripts("sw-push.js")`](../evidence/C-024/sw-import.log)
- Fixed a real bug found during verification: `queue_push_event()` in `supabase/push.sql` had a
  PL/pgSQL variable colliding with the `push_deliveries.event_id` column, raising "ambiguous
  column reference" on every roommate-change trigger fire.
- Client wiring: `NotificationModal.tsx` now drives the real `pushClient` subscribe/unsubscribe
  flow instead of the previous mock endpoint/keys; `App.tsx` calls `pushClient.setSession(...)`
  on every session change so room-switch/logout cleanup runs.
- Open (needs live access this session did not have): a real browser executing the service
  worker's push/notificationclick handlers; an actual notification received on a locked/closed
  device from a deployed, `VAPID_*`/`CRON_SECRET`-configured Production scheduler. No production
  promotion was made.

### Update 2026-09-03: live infrastructure connected

Operator provided a real Supabase project. Migrated it live (`supabase/schema.sql` +
`supabase/push.sql`, all 3 versions applied), configured real `DATABASE_URL`, `SUPABASE_*`,
`VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`, `CRON_SECRET` as Vercel Preview secrets,
and deployed (`dpl_CY1swekHCVbTWWDBqiiNxvGYYdFh`). Live-verified: `/readyz` durable database,
`/api/config` reports `push:true`/`realtime:true`, real subscribe accepted/rejected correctly,
cron bearer auth correct, and a real subscribe→mutate→dispatch cycle against Google's actual FCM
endpoint produced a real `HTTP_410` and correctly disabled the subscription (expired-endpoint
cleanup, proven against a live provider, not a mock). Test room deleted afterward; no other data
existed in the project. Full record: [evidence/C-024/live-2026-09-03.md](../evidence/C-024/live-2026-09-03.md).

Still blocked: Vercel Cron Jobs execute only on **Production**, and this Preview has Deployment
Protection (SSO) enabled, which would also block a phone's browser from opening the install page.
Receiving an actual notification on a physical device needs an operator decision on how to expose
a testable URL (see conversation) before it can be closed. Card stays `todo`.
