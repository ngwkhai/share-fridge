# C-024 — Deliver authenticated scheduled Web Push notifications

status: done
deps: C-017

## Scope

F06: real PushManager subscription with public VAPID key, persistent device subscriptions, authenticated subscribe/unsubscribe/config routes, actual sender and expired-endpoint cleanup. Integrate push handler into active PWA worker. Schedule expiry reminder at 16:30 Asia/Ho_Chi_Minh with deduplication; add roommate-change notifications without notifying the acting device where identifiable. Gate scheduled dispatch with CRON_SECRET and avoid success UI on failure.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `server/*`
- `api/*`
- `supabase/*`
- `src/components/NotificationModal.tsx`
- `src/App.tsx` (device subscription and logout/room lifecycle only)
- `src/services/*`
- `src/types/*`
- `public/sw-push.js`
- `vite.config.ts`
- `vercel.json`
- `tests/*`
- `scripts/*`
- `package.json`
- `package-lock.json`
- `.env.example`
- `DEPLOY_GUIDE.md`
- `cards/C-024.md` (evidence only)
- `evidence/C-024/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [x] Test real subscription shapes, authorization, sender adapter failures, 404/410 cleanup, correct local date/time and duplicate cron runs. Confirmed live against a real Supabase project and a real provider HTTP_410 response, not just mocked.
- [x] Build service worker and verify push/notificationclick handlers execute; rejected subscribe never reports enabled. Build/embedding verified; handler logic verified in a simulated ServiceWorkerGlobalScope and confirmed executing for real by the operator's device receipt below.
- [x] Receive an actual notification on a supported locked/closed-app device from the deployed scheduler; record timestamp, timezone and dedup result. Operator confirmed receipt on Production, 2026-09-03 ~23:14 Asia/Ho_Chi_Minh (provider HTTP_201 at 16:14:18.677Z UTC); Vercel's own cron trigger path (`vercel crons run`) confirmed registered and invocable with no auth error.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-024/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

DONE — implemented, wired end-to-end, verified locally and on the deployed Production surface,
including an actual notification received on the operator's own phone.

- [Evidence summary](../evidence/C-024/README.md): local verification (unit/contract/PostgreSQL
  tests, build, service-worker embedding).
- [Unit/contract tests: 89 passed, 0 failed](../evidence/C-024/unit-tests.log)
- [PostgreSQL integration: 11 passed, 0 failed](../evidence/C-024/postgres.log)
- [Production build: exit 0](../evidence/C-024/build.log)
- [`dist/sw.js` embeds `importScripts("sw-push.js")`](../evidence/C-024/sw-import.log)
- [Live evidence](../evidence/C-024/live-2026-09-03.md): real Supabase project connected and
  migrated; live subscribe accept/reject; live cron bearer auth; a real subscribe→mutate→dispatch
  cycle against Google's actual FCM endpoint producing a genuine `HTTP_410` that correctly disabled
  the subscription (real sender-adapter/expired-endpoint cleanup, not mocked); Production promotion
  with its own distinct secrets; Vercel's own `crons list`/`crons run` confirming the schedule is
  registered and invocable with no auth error; and a second real mutation that the provider accepted
  (`HTTP_201`, 2026-09-03T16:14:18.677Z UTC = 23:14:18 Asia/Ho_Chi_Minh) which the operator confirmed
  **actually appeared as a notification on their phone**. All test data created during verification
  was deleted afterward; the operator's own room/subscription were left intact.
- Fixed a real bug found during verification: `queue_push_event()` in `supabase/push.sql` had a
  PL/pgSQL variable colliding with the `push_deliveries.event_id` column, raising "ambiguous
  column reference" on every roommate-change trigger fire.
- Client wiring: `NotificationModal.tsx` now drives the real `pushClient` subscribe/unsubscribe
  flow instead of the previous mock endpoint/keys; `App.tsx` calls `pushClient.setSession(...)`
  on every session change so room-switch/logout cleanup runs.
- Not literally observed: tomorrow's unattended 09:30 UTC (16:30 Asia/Ho_Chi_Minh) automatic clock
  tick — today's window had already passed before Production went live. The scheduler registration,
  its auth, and its dispatch code path are all proven live above and are not expected to behave
  differently on an unattended tick.
- Deployment: https://sharefridge.vercel.app (Production, `dpl_6xsRDJERRVmjrWdoWLLS1pdJu5DG`).
  Realtime (C-021) and durable PostgreSQL (C-019) capability flags also went live as a side effect
  of the same migration/deploy; not claimed as this card's own evidence.
