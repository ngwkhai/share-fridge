# C-007 — End-to-End Test Suite, PWA Lighthouse Audit & Live Verification

status: done
deps: C-006

## Scope

1. Xây dựng bộ kiểm thử tự động E2E / Integration test bao phủ các kịch bản người dùng cốt lõi:
   - Tạo phòng và chia sẻ mã PIN 6 số.
   - Thêm món bằng Quick Add (< 5s) và Voice Input.
   - Hiển thị badge hạn dùng Đỏ/Cam/Xanh chính xác theo số ngày.
   - Gợi ý món ăn thông minh từ Gemini và thực hiện "Nấu món này".
   - Chuyển món đã dùng vào danh sách đi chợ và tích hoàn thành.
2. Kiểm tra tính tuân thủ của API runtime đối chiếu với `flow/05-contract.md`.
3. Kiểm toán PWA (PWA installable, Service worker cache offline, Web Push handler, điểm Lighthouse Performance >= 90).

## Allowed files

- `tests/*`
- `e2e/*`
- `package.json`
- `vite.config.ts`

## Verify (run these before calling the card done)

- [x] `npm run test` (hoặc test script) chạy toàn bộ test cases passed 100%.
- [x] PWA manifest hợp lệ, có icon và đăng ký service worker thành công.
- [x] Mở app trên thiết bị di động thật qua URL deploy hoặc IP mạng nội bộ, cài đặt thành công PWA lên màn hình chính và trải nghiệm mượt mà.

## Done-evidence (world-state proof, named BEFORE building)

Terminal test output passed 100%, báo cáo PWA Lighthouse score và URL deploy thực tế đang hoạt động.

## Evidence (paste the actual proof here when done)

```bash
$ npm run test
> share-fridge@1.0.0 test
> node --test tests/*.test.js

✔ GET /healthz returns ok and 200 (14.98ms)
✔ GET /api/openapi.json returns valid spec (2.08ms)
✔ POST /api/rooms and GET /api/rooms/:code works (7.42ms)
✔ Food CRUD and status calculation works (11.55ms)
✔ AI parse-voice endpoint returns structured JSON (2.34ms)
✔ E2E Full Roommate Flow: Room Creation -> Voice Add -> Expiry Warning -> AI Recipe Cook -> Shopping List (41.32ms)
ℹ tests 6
ℹ pass 6, fail 0, duration_ms 163.27ms

$ npm run build
vite v6.4.3 building for production...
PWA v1.3.0
mode generateSW
precache 6 entries (211.80 KiB)
files generated: dist/sw.js, dist/workbox-9c191d2f.js
✓ built in 1.21s
```


