# C-021 — Prevent cache resurrection and reconnect room synchronization

status: todo
deps: C-017

## Scope

F05/F10: remove silent room recreation/reseeding; treat successful empty snapshots as authoritative; never convert network/auth failure into empty data; maintain absolute expiry and stable IDs. Guard against stale responses after mutation/logout/room changes. Clear invalid sessions, sync history and shopping, refresh on visibility/reconnect and show accurate connection status. Implement authenticated Supabase Realtime when configured, with an explicit degraded polling fallback; do not pretend polling meets the 500ms target. Existing writes stay online-only unless a fully tested outbox is implemented.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `src/App.tsx`
- `src/services/*`
- `src/hooks/*`
- `src/components/Header.tsx`
- `src/types/*`
- `server/*`
- `supabase/*`
- `tests/*`
- `package.json`
- `package-lock.json`
- `.env.example` (document public/server Realtime settings without secrets)
- `DEPLOY_GUIDE.md` (Realtime setup and honest degraded-mode requirements)
- `cards/C-021.md` (evidence only)
- `evidence/C-021/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [ ] Test exact cache regression: delete final item on device A, device B reload/reconnect must stay empty; no new ID or expiry extension.
- [ ] Test two concurrent sessions, history updates, stale responses and logout; show offline/reconnecting/connected accurately.
- [ ] On two real browser contexts using the deployed DB, measure and document change latency and reconnect; realtime must meet the PRD target before closing this card.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-021/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

PARTIAL — implementation and local checks complete; live gate remains open.

- [Planner review and local browser proof](../evidence/C-021/planner-review.md): independent tests/build passed, first-load rendering finding corrected by a fresh builder, actual two-tab delete/reload remained empty and PostgreSQL retained zero matching rows.

- [Evidence summary](../evidence/C-021/README.md): 55 local tests after the review fix, 3 new PostgreSQL tests, 11 persistence/security regressions, 4 client/PostgreSQL regressions, all passing with no skips.
- [Unit/client/controller log](../evidence/C-021/tests.log), [revision/RLS log](../evidence/C-021/postgres.log), [build log](../evidence/C-021/build.log).
- Actual tests demonstrate final-item deletion stays empty after reload/reconnect, history/shopping clears, IDs/expiry are preserved, and stale reads/writes/callbacks cannot restore a previous session.
- Fresh-agent review fixed the initial App rendering defect. [Actual React tests](../evidence/C-021/review-fix-tests.log) verify loading/error/retry before all empty states, including legacy room-less sessions, session changes on history/shopping tabs, and preserved stale snapshots. [Build](../evidence/C-021/review-fix-build.log) passes.
- Hosted Supabase is not configured in this session. Two deployed browser contexts, JWT acceptance, reconnect and <500 ms latency remain unverified; status stays todo. No polling latency is presented as Realtime proof.

[Live evidence, 2026-09-04](../evidence/C-021/live-2026-09-04.md): now that Supabase is connected on Production, ran the deployed-DB test with two real browser contexts (both genuinely subscribed to the Realtime channel, not degraded polling). Reconnect (offline->online) passes cleanly. **Latency does NOT meet the PRD's <500ms target** — measured 1231-3169ms across 3 reproducible runs, using the server's own write timestamp vs. the receiving tab's detection time. Verify item 3 stays unchecked and this card stays `todo`; closing the gap needs an operator decision (accept as DEBT vs. switch transport to Supabase Broadcast), see the live evidence file's "Open question" section.

Formally logged as [DEBT](../DEBT.md) on 2026-09-04 per CLAUDE.md's gate-skip protocol: not a security-class skip, so no Tier-C halt required, but written down rather than silently carried.

**Same-day update, with operator approval**: implemented and applied the Broadcast-API transport switch (`supabase/realtime_broadcast.sql`, purely additive alongside the unchanged `postgres_changes` fallback). Result: inconclusive latency improvement -- root cause turned out to be REST round-trip latency (0.8-1.3s per request, measured directly, independent of realtime) dominating the total, not the realtime transport. Broadcast change is kept (safe, additive) but does not close this card. See [DEBT.md](../DEBT.md)'s update and [live evidence](../evidence/C-021/live-2026-09-04.md)'s "Broadcast attempt" section. Card stays todo.

**Follow-up local remediation, 2026-09-04**: added a direct, validated room-delta fast path that is emitted immediately after a REST mutation succeeds, before the source device waits for its full snapshot refresh. The server-triggered invalidation/full refresh remains the authority and reconciliation mechanism. [Local build/regression evidence](../evidence/C-021/low-latency-delta-local-2026-09-04.md) records 24 targeted tests passing. This is intentionally not treated as live acceptance; two real Production browser contexts must measure the resulting visible peer latency before this card can move from `todo`.

**Measured follow-up, 2026-09-04**: verified the current Production bundle in two real tabs, then measured click-to-peer-visible at **3143 / 2592 / 2644 ms** (median 2644 ms). These tabs share session storage; isolated-context acceptance is still open. One-item deletion survived receiver reload; this is not the final-item-empty test. [Detailed measurements, limitations, local fixes and remaining-card acceptance sequence](../evidence/C-021/measured-acceptance-2026-09-04.md). Three local fast-path correctness fixes pass **118/118** tests and build, but are not yet deployed. Correction to prior evidence: `added_date` is created before the database write, not a commit timestamp; synthetic offline events do not prove real network recovery. Status remains `todo`.
