# C-019 — Durable PostgreSQL repository and isolated room data

status: todo
deps: C-017

## Scope

F03: replace production Map storage with an asynchronous PostgreSQL repository, transactions, migration tooling and readiness. Use DATABASE_URL and pooled connections; test with a disposable real local PostgreSQL database. No production fallback to RAM. Room sessions remain the access contract. Migration must revoke public table access and apply room isolation; preserve existing rows and make migrations repeatable. Add shared rate-limit persistence and durable subscriptions support.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `server/*`
- `supabase/*`
- `api/*`
- `scripts/*`
- `tests/*`
- `package.json`
- `package-lock.json`
- `.env.example`
- `.gitignore`
- `DEPLOY_GUIDE.md`
- `vercel.json` (route /readyz to API; exclude readiness from SPA fallback)
- `cards/C-019.md` (evidence only)
- `evidence/C-019/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [x] Run migrations twice on a disposable PostgreSQL database; verify two independent API instances and restart preserve room, foods, shopping and subscriptions.
- [x] Verify room isolation and transactional/idempotent consume; missing production DATABASE_URL fails readiness without exposing internals.
- [ ] On the linked deployment, insert/read through two requests and verify durable DB rows and reconnect after redeploy; paste sanitized results and spec URL.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-019/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

PARTIAL — implemented and verified locally; status remains `todo`.

- [Evidence summary](../evidence/C-019/README.md)
- [PostgreSQL integration: 11 passed, 0 skipped](../evidence/C-019/postgres.log)
- [Unit/API: 32 passed, 0 skipped](../evidence/C-019/unit.log)
- [Production build succeeded](../evidence/C-019/build.log)

Hosted persistence verification is blocked by missing hosted `DATABASE_URL`; no live DB/redeploy evidence is claimed. Planner independently reviewed allowed files, repository transactions, migration/RLS and routing; rerun logs: evidence/C-019/planner-unit.log (32 passed), planner-postgres.log (11 passed), planner-build.log (build passed). Final git diff --check passed. Hosted gate remains open.
