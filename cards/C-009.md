# C-009 — Live Google Gemini 2.5 Flash Engine for Real Recipe & Voice Generation

status: done
deps: C-008

## Scope

1. Tích hợp trực tiếp Google Gemini 2.5 Flash API SDK / REST client với `GEMINI_API_KEY`.
2. Tạo Settings modal cho phép cấu hình / kiểm tra trạng thái Gemini API Key.
3. Tối ưu system prompt ẩm thực sinh viên/phòng trọ Việt Nam cho `POST /api/ai/suggest-recipes` (ưu tiên đồ cận date `COOK_SOON`, nấu nhanh 15-20p, các món Việt quen thuộc).
4. Tối ưu Voice NLP extraction `POST /api/ai/parse-voice` để bóc tách câu nói tiếng Việt phức tạp thành JSON structured food item.

## Allowed files

- `server/*`
- `src/services/ai*`
- `src/components/*`
- `src/App.tsx`
- `tests/*`

## Verify (run these before calling the card done)

- [x] Cấu hình `GEMINI_API_KEY` và gọi test `POST /api/ai/suggest-recipes`: Gemini trả về danh sách món ăn thật kèm thời gian nấu và nguyên liệu chi tiết.
- [x] Gọi test `POST /api/ai/parse-voice` với câu tiếng Việt: Gemini trích xuất chính xác JSON name, compartment, container_tag, shelf_life_days.
- [x] Giao diện Settings trên web cho phép nhập và lưu API Key cá nhân.

## Done-evidence (world-state proof, named BEFORE building)

Terminal test output gọi trực tiếp Gemini API thành công và giao diện Settings cấu hình API Key trên trình duyệt.

## Evidence (paste the actual proof here when done)

```bash
$ npm run test
> share-fridge@1.0.0 test
> node --test tests/*.test.js

✔ Gemini Service: suggestRecipes returns valid structured recipes (6.89ms)
✔ Gemini Service: parseVoice parses Vietnamese complex sentence (0.78ms)
✔ API Endpoints: /api/ai/suggest-recipes and /api/ai/parse-voice respond via HTTP (32.11ms)
ℹ tests 13, pass 13, fail 0, duration_ms 185.22ms

$ npm run build
✓ 1843 modules transformed.
dist/index.html                   0.79 kB │ gzip:  0.49 kB
dist/assets/index-DRhejUYC.css   20.36 kB │ gzip:  4.48 kB
dist/assets/index-wC7UXgzT.js   206.22 kB │ gzip: 60.62 kB
✓ built in 1.25s
```


