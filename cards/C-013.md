# C-013 — Senior Glassmorphism Design System, Animations & Copy Cleanup

status: done
deps: C-012

## Scope

1. Cập nhật `tailwind.config.js` và `src/index.css` với bảng màu neon gradient, hiệu ứng kính mờ `glass-card`, `glass-dock`, và keyframes animation (spring bounce, ambient neon pulse, breathing glow).
2. Tinh chỉnh và nâng cấp toàn bộ UI components:
   - `Header`: Kính mờ siêu mỏng, avatar logo phát sáng, room badge kim loại.
   - `PulseStrip`: 4 thẻ kính màu neon nổi bật.
   - `FilterBar`: Chip lọc tương tác mượt mà.
   - `FoodCard`: Thẻ bo cong 24px, pill đếm ngược ngày phát sáng neon cam/đỏ/xanh.
   - `QuickAddModal`: Bottom sheet tối giản, loại bỏ text thừa, dùng segmented controls.
   - `VoiceInputModal`: Sóng âm đa vòng tròn phát sáng kiểu Siri (Multi-Ring Neon Visualizer).
   - `RecipeModal`: Thẻ món ăn đẳng cấp đầu bếp, 1 chạm nấu ngay.
   - `BottomNav`: Floating Glass Dock bay lơ lửng với hào quang phát sáng.
3. Xóa bỏ triệt để tất cả các đoạn text giải thích dài dòng, note thừa thãi trên màn hình Login, Empty states và Modals.

## Allowed files

- `tailwind.config.js`
- `src/index.css`
- `src/components/*`
- `src/App.tsx`
- `tests/*`

## Verify (run these before calling the card done)

- [x] `npm run build` chạy thành công không có lỗi CSS / TypeScript.
- [x] Mở app trên mobile view: Giao diện Glassmorphism với màu sắc sống động, Floating Dock bay nổi mượt mà.
- [x] Không còn bất kỳ đoạn text hướng dẫn dài thừa thãi nào trên các màn hình.
- [x] Chạy `npm run test`: Toàn bộ 16 test cases vượt qua 100%.

## Done-evidence (world-state proof, named BEFORE building)

Terminal output `npm run test` passed 100%, output `npm run build` và kiểm tra trực quan giao diện Glassmorphism trên trình duyệt.

## Evidence (paste the actual proof here when done)

```bash
$ npm run test
> share-fridge@1.0.0 test
> node --test tests/*.test.js

ℹ tests 16, pass 16, fail 0, duration_ms 190.45ms

$ npm run build
vite v6.4.3 building for production...
✓ 1844 modules transformed.
dist/index.html                   0.98 kB │ gzip:  0.55 kB
dist/assets/index-BzQUdqJX.css   31.05 kB │ gzip:  5.97 kB
dist/assets/index-BzRRG6XK.js   206.82 kB │ gzip: 61.24 kB
✓ built in 1.09s
PWA v1.3.0
files generated: dist/sw.js, dist/workbox-9c191d2f.js

Giao diện Senior UI/UX hoàn tất:
- Frosted Glassmorphism, Floating Dock, Siri Multi-Ring Wave Visualizer, Neon Ambient Glow.
- Đã loại bỏ hoàn toàn các đoạn text giải thích dài thừa thãi.
```


