# C-026 — Complete food-card editing and accessible mobile interactions

status: todo
deps: C-017

## Scope

F12: replace undefined Tailwind classes with supported tokens; honor existing DESIGN structure and 44px targets/reduced motion. Accessible shared dialogs with labels/focus/Escape and labeled icon controls. Food edit/consume/delete pending/error handling with non-destructive recovery; clear new voice drafts completely; await clipboard success. Enable zoom and mobile safe areas. Preserve current visual direction rather than inventing a new mock theme.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `src/*`
- `tailwind.config.js`
- `index.html`
- `tests/*`
- `package.json`
- `package-lock.json`
- `cards/C-026.md` (evidence only)
- `evidence/C-026/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [ ] npm run build and component/browser tests cover actual interactions, async failures, keyboard focus and reset of form/voice drafts.
- [ ] Review at 390/430px, long text, zoom/reduced motion and image states; record screenshots from the in-app browser.
- [ ] Verify the same interaction cases on the deployed URL and attach screenshots/trace; no success message when server/clipboard rejects.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-026/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

(empty until done)
