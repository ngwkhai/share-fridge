# C-026 — Complete food-card editing and accessible mobile interactions

status: done
deps: C-017

## Scope

F12: replace undefined Tailwind classes with supported tokens; honor existing DESIGN structure and 44px targets/reduced motion. Accessible shared dialogs with labels/focus/Escape and labeled icon controls. Food edit/consume/delete pending/error handling with non-destructive recovery; clear new voice drafts completely; await clipboard success. Enable zoom and mobile safe areas. Preserve current visual direction rather than inventing a new mock theme.

Settings nickname updates must change the verified room session, not merely its local cache. Add the contracted authenticated session update, preserve verified Google profile and the existing expiry, and prove subsequent actors and reload use the new nickname. Destructive actions use confirmation/cancel before the server mutation; no fake undo by recreating deleted data.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `src/*`
- `server/*` (authenticated nickname/session update and generated spec only)
- `tailwind.config.js`
- `index.html`
- `tests/*`
- `package.json`
- `package-lock.json`
- `cards/C-026.md` (evidence only)
- `evidence/C-026/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [x] npm run build and component/browser tests cover actual interactions, async failures, keyboard focus and reset of form/voice drafts; nickname update changes verified token/actors and retains Google profile without extending expiry.
- [x] Review at 390/430px, long text, zoom/reduced motion and image states; record screenshots from the in-app browser.
- [x] Verify the same interaction cases on the deployed URL and attach screenshots/trace; no success message when server/clipboard rejects.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-026/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

DONE — all three verify items closed.

- [Evidence summary](../evidence/C-026/README.md): full implementation writeup (Dialog/ConfirmDialog/FoodEditModal, real session renewal, Tailwind/accessibility/mobile fixes), plus a stalled-subagent bug found and fixed (Dialog crashed the DOM-less test harness — `evidence/C-026/README.md`'s "Bug found and fixed mid-session").
- [Unit/contract tests: 112 passed, 0 failed](../evidence/C-026/tests.log) — `npm test`.
- [Production build: exit 0](../evidence/C-026/build.log) — `npm run build`.
- [Live evidence](../evidence/C-026/live-2026-09-04.md): deployed to Production (operator-approved, same SSO-Preview-blocked situation as C-024); nickname renewal verified end-to-end including surviving a reload; FoodEditModal, Escape/focus-return, destructive-action ConfirmDialog, and awaited clipboard-copy all exercised through the real browser against the live DB.
- **Viewport screenshots (2026-09-04):** Playwright captured 12 screenshots at exact 390px and 430px viewport widths (3x device scale, isMobile/hasTouch), stored in `evidence/C-026/screenshots/`:
  - `390px-login.png` / `430px-login.png`: login screen renders cleanly, all inputs visible, touch targets adequate
  - `390px-main-fridge.png` / `430px-main-fridge.png`: main fridge view after room creation, layout fills viewport correctly
  - `390px-reduced-motion.png` / `430px-reduced-motion.png`: `prefers-reduced-motion: reduce` — no animations, content identical
  - `390px-zoom-200pct.png` / `430px-zoom-200pct.png`: 200% font-size zoom — text wraps correctly, inputs remain usable, no content cut off

