# C-023 — Validate AI results and cook recipes by exact food identity

status: todo
deps: C-017

## Scope

F08/F09: exclude EXPIRED/CONSUMED food; validate provider JSON and inventory membership; expose source and timeout/fallback behavior. Improve Vietnamese heuristic extraction including half-unit quantities, explicit storage and packaging. Suggestions include food_ids; implement atomic batch cook endpoint with idempotency and concurrency protection. Show missing ingredients, source and actionable errors. Real-provider smoke must distinguish unavailable configuration from pass.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `server/*`
- `src/components/RecipeModal.tsx`
- `src/components/VoiceInputModal.tsx`
- `src/components/QuickAddModal.tsx`
- `src/services/*`
- `src/types/*`
- `src/App.tsx`
- `supabase/*`
- `tests/*`
- `scripts/*`
- `package.json`
- `package-lock.json`
- `cards/C-023.md` (evidence only)
- `evidence/C-023/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [ ] Run fixture cases for malformed provider output, missing/expired/out-of-room ingredients, Vietnamese extraction and request timeout.
- [ ] Test atomic cooking: all chosen IDs transition once; concurrent/replayed operation cannot partially consume or duplicate shopping items.
- [ ] On deployed environment with GEMINI_API_KEY, capture sanitized provider source/results and cooking DB effects; fallback-only tests cannot close provider acceptance.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-023/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

(empty until done)
