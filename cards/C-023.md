# C-023 — Validate AI results and cook recipes by exact food identity

status: done
deps: C-017

## Scope

F08/F09: exclude EXPIRED/CONSUMED food; validate provider JSON and inventory membership; expose source and timeout/fallback behavior. Improve Vietnamese heuristic extraction including half-unit quantities, explicit storage and packaging. Suggestions include food_ids; implement atomic batch cook endpoint with idempotency and concurrency protection. Show missing ingredients, source and actionable errors. Real-provider smoke must distinguish unavailable configuration from pass.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `server/*`
- `src/components/RecipeModal.tsx`
- `src/components/VoiceInputModal.tsx`
- `src/components/QuickAddModal.tsx`
- `src/services/*`
- `src/types/*`
- `src/App.tsx`
- `supabase/*`
- `tests/*`
- `scripts/*`
- `package.json`
- `package-lock.json`
- `cards/C-023.md` (evidence only)
- `evidence/C-023/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [x] Run fixture cases for malformed provider output, missing/expired/out-of-room ingredients, Vietnamese extraction and request timeout.
- [x] Test atomic cooking: all chosen IDs transition once; concurrent/replayed operation cannot partially consume or duplicate shopping items.
- [x] On deployed environment with GEMINI_API_KEY, capture sanitized provider source/results and cooking DB effects; fallback-only tests cannot close provider acceptance.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-023/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

- [Planner independent review](../evidence/C-023/planner-review.md): 72 application tests, 24 PostgreSQL tests and build passed; actual local browser voice-draft-save-recipe-cook-history matches exact database ID and durable replay.

- [Local evidence](../evidence/C-023/README.md): 72 unit/API/actual-client/rendered-React tests passed; 6 two-process PostgreSQL tests passed; build and diff check passed.
- [Tests](../evidence/C-023/tests.log), [PostgreSQL](../evidence/C-023/postgres.log), [build](../evidence/C-023/build.log).
- [Paid-for lessons](../playbooks/gemini-atomic-cooking.md).
- [Live evidence, 2026-09-04](../evidence/C-023/live-2026-09-04.md): root-caused the "configured key still fails" gap (the model `gemini-2.5-flash` was deprecated by Google for new API keys — not a bad key, diagnosed by curling Google's API directly and reading its real error message, since this app deliberately never logs provider payloads). Switched to `gemini-3.1-flash-lite` after `gemini-3.6-flash` proved technically correct but too slow (8-30s per call due to default "thinking" mode). Live on Production: real Gemini completions for both `/api/ai/parse-voice` (0.98 confidence, correct Vietnamese quantity/container/shelf-life extraction) and `/api/ai/suggest-recipes` (real coherent recipe using both room foods by exact id), then actually cooked a Gemini-named `food_id` through `/api/foods/consume-batch` and confirmed the DB moved it from active to consumed with correct `consumed_by`/`consumed_at`. All test data cleaned up.
