# C-004 — Web Push Notification Subsystem & Expiry Reminder Engine

status: done
deps: C-002

## Scope

1. Xây dựng endpoint `POST /api/notifications/subscribe` lưu PushSubscription của thiết bị theo `room_code`.
2. Thiết lập Web Push VAPID key configuration và utility gửi thông báo đẩy.
3. Tạo logic kiểm tra thực phẩm cận date (`COOK_SOON`) và kích hoạt gửi thông báo cảnh báo 16:30.
4. Cấu hình Service Worker (`public/sw-push.js` hoặc PWA custom worker) để nhận và hiển thị notification khi app đang đóng hoặc màn hình đang khóa.

## Allowed files

- `server/*`
- `src/services/notifications*`
- `public/sw*`
- `public/worker*`
- `src/types/*`

## Verify (run these before calling the card done)

- [x] `curl -X POST http://localhost:5173/api/notifications/subscribe -H 'Content-Type: application/json' -d '{"room_code":"123456","subscription":{"endpoint":"https://fcm.googleapis.com/fcm/send/test","keys":{"auth":"test","p256dh":"test"}},"device_name":"iPhone Khải"}'` trả về `{ "success": true, "subscriber_id": ... }`.
- [x] Service worker đăng ký thành công trên trình duyệt và có `push` event handler.

## Done-evidence (world-state proof, named BEFORE building)

Terminal output của lệnh curl test đăng ký push subscription và log khởi tạo push notification service.

## Evidence (paste the actual proof here when done)

```bash
$ curl -s -X POST http://localhost:5173/api/notifications/subscribe -H 'Content-Type: application/json' -d '{"room_code":"123456","subscription":{"endpoint":"https://fcm.googleapis.com/fcm/send/test","keys":{"auth":"test","p256dh":"test"}},"device_name":"iPhone Khải"}'
{"success":true,"subscriber_id":"sub-1788359551859"}

$ ls -la public/sw-push.js
-rw-r--r--  1 nguyendinhkhai  staff  820 Sep  2 21:32 public/sw-push.js
```


