# C-025 — Store bounded food photos and ship correct PWA assets

status: done
deps: C-017

## Scope

F11: client Blob compression measures bytes and bounds both dimensions; reject/correct invalid or oversized images; authenticated Supabase Storage upload and signed URLs, no durable base64 in food records. Handle failed upload/retry and orphan cleanup. Generate actual PNG 192/512 icons from existing logo with image tooling (no AI edit), unify manifest source and verify install assets.

Audit reference: audit/2026-09-03/REPORT.md. This card repairs an existing C-001..C-017 capability against the amended contract; C-018..C-026 have no build-time dependency on each other and are executed serially because files overlap. This does not waive live verification. Integration/release cards depend on all remediation cards.

## Allowed files

- `src/components/CameraCapture.tsx`
- `src/components/QuickAddModal.tsx`
- `src/components/FoodCard.tsx` (unavailable/expired photo placeholder only)
- `src/services/*`
- `src/types/*`
- `server/*`
- `supabase/*`
- `scripts/*`
- `public/*`
- `index.html`
- `vite.config.ts`
- `tests/*`
- `package.json`
- `package-lock.json`
- `.env.example`
- `DEPLOY_GUIDE.md`
- `cards/C-025.md` (evidence only)
- `evidence/C-025/*`
- `playbooks/*` (proven lessons only)

## Verify (run these before calling the card done)

- [x] Test compressed byte limit <=100KB on portrait/landscape/detail images and failed reads/uploads; validate server MIME/signatures and room ownership.
- [x] Verify PNG magic bytes and exact dimensions; one generated manifest; npm run build passes.
- [x] Upload a real image from device A and view it on B through deployed storage; verify private access and install PWA with correct icons.

## Done-evidence (world-state proof, named BEFORE building)

A working deployed surface on the existing authorized project, with sanitized HTTP/DB outputs and browser/provider evidence matching every Verify item. Store reproducible commands/artifacts under evidence/C-025/ and link them here. Local tests are PARTIAL evidence only. Missing live access or provider configuration keeps status todo and is recorded in AUTO-LOG.md.

## Evidence (paste the actual proof here when done)

Full writeup: `evidence/C-025/README.md`.

- Unit/contract tests (106 passed incl. photo validation/upload/replay/409/remove/signFoods/
  cleanup and PWA icon/manifest checks): `evidence/C-025/unit-tests.log`.
- Production build exit 0: `evidence/C-025/build.log`.
- PostgreSQL integration incl. photo attach/release atomicity, idempotency, cleanup sweep:
  `evidence/C-025/postgres.log`, `evidence/C-025/photos-postgres.log`.
- Live Production (`https://sharefridge.vercel.app`): real Supabase Storage bucket
  `food-photos` (private) confirmed, migration applied, real upload -> signed URL -> byte-level
  download (JPEG 300x200, 638 bytes) -> attach to food -> freshly-signed read on GET, delete ->
  cron cleanup -> object actually removed from bucket, test room cleaned up:
  `evidence/C-025/live-2026-09-03.md`.
- Pre-fix defect baseline (existing broken PNG/manifest before this card's build):
  `evidence/C-025/live-baseline.md`.
- Operator real-device confirmation (2026-09-04, after the `w-13`/`h-13` Tailwind fix in
  e2df3ac): food-photo thumbnail renders at the correct 52px box, and the installed PWA
  home-screen icon shows the real logo — `evidence/C-025/device-confirmation-2026-09-04.md`.
