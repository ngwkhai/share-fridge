# C-017 — Google Sign-In & User Identity Integration

status: done
deps: C-016

## Scope

1. Xây dựng component `src/components/GoogleAuthButton.tsx` chuẩn nhận diện thương hiệu Google (Google SVG Logo, One-Tap / OAuth handler).
2. Tích hợp trích xuất thông tin Google Profile (`name, email, picture, sub`) vào Session Cache.
3. Tích hợp nút Google Sign-In vào màn hình Chào mừng / Đăng nhập trong `src/App.tsx`.
4. Hiển thị Avatar Google trên Header và modal Cài đặt khi người dùng đăng nhập bằng Google.
5. Viết unit tests kiểm thử luồng giải mã payload Google Auth và tích hợp session.

## Allowed files

- `src/components/GoogleAuthButton.tsx`
- `src/components/Header.tsx`
- `src/components/SettingsModal.tsx`
- `src/App.tsx`
- `src/services/api.ts`
- `tests/*`

## Verify (run these before calling the card done)

- [x] Màn hình Đăng nhập hiển thị nút "Tiếp tục với Google" chuẩn thiết kế Google.
- [x] Bấm Đăng nhập Google: Tên và Avatar được tự động trích xuất và hiển thị ngay trên Header.
- [x] `npm run build` và `npm run test` vượt qua 100%.

## Done-evidence (world-state proof, named BEFORE building)

Kiểm tra nút Google Sign-In và Avatar Google hiển thị trên giao diện, output `npm run build` & `npm run test`.

## Evidence (paste the actual proof here when done)

```bash
$ npm run test
> share-fridge@1.0.0 test
> node --test tests/*.test.js

✔ Google Auth & Profile Session: Validates profile fields and structure (0.46ms)
ℹ tests 18, pass 18, fail 0, duration_ms 212.92ms

$ npm run build
vite v6.4.3 building for production...
✓ 1845 modules transformed.
dist/index.html                   0.98 kB │ gzip:  0.55 kB
dist/assets/index-BmhlmDnE.css   31.27 kB │ gzip:  5.96 kB
dist/assets/index-QBk_tY_J.js   217.05 kB │ gzip: 63.95 kB
✓ built in 1.01s
PWA v1.3.0
files generated: dist/sw.js, dist/workbox-9c191d2f.js

Tích hợp Google Sign-In hoàn tất:
- Nút bấm 'Tiếp tục với Google' chuẩn SVG thương hiệu Google.
- Tự động lấy Họ tên, Avatar, Email và hiển thị trên Header và Modal Cài đặt.
```


