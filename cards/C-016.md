# C-016 — Local-First Persistent Storage & Nickname In-Room Display

status: done
deps: C-015

## Scope

1. Xây dựng `foodCache` helper trong `src/services/api.ts` (lưu trữ danh sách foods và shopping items vào Local Storage theo từng `roomCode`).
2. Khắc phục triệt để lỗi chớp/mất dữ liệu trong `src/App.tsx`: Khi Serverless Lambda trả về danh sách rỗng, client bảo vệ local state và tự động đồng bộ ngược lại server thay vì xóa mất dữ liệu trên màn hình.
3. Đưa Tên người dùng (`nickname`) hiển thị rõ ràng trên `src/components/Header.tsx` (`Khải • Phòng 302 #839201`).
4. Gán `created_by: nickname` khi người dùng thêm món trong `QuickAddModal.tsx` và hiển thị nickname trong `SettingsModal.tsx`.

## Allowed files

- `src/services/api.ts`
- `src/App.tsx`
- `src/components/Header.tsx`
- `src/components/SettingsModal.tsx`
- `src/components/QuickAddModal.tsx`
- `tests/*`

## Verify (run these before calling the card done)

- [x] Mở app và thêm 3 món: Sau nhiều chu kỳ polling 4s, dữ liệu luôn hiển thị ổn định 100%, không bị chớp hay biến mất.
- [x] Header hiển thị rõ ràng Tên người dùng và Tên phòng (`Khải • Phòng 302 #839201`).
- [x] Thêm món mới: Món hiển thị tag tác giả đúng với tên người dùng đã đăng nhập.
- [x] `npm run build` và `npm run test` chạy thành công.

## Done-evidence (world-state proof, named BEFORE building)

Kiểm tra trực quan Header hiển thị Tên người dùng và dữ liệu ổn định không bị chớp mất sau nhiều chu kỳ polling.

## Evidence (paste the actual proof here when done)

```bash
$ npm run test
> share-fridge@1.0.0 test
> node --test tests/*.test.js

ℹ tests 17, pass 17, fail 0, duration_ms 212.80ms

$ npm run build
vite v6.4.3 building for production...
✓ 1844 modules transformed.
dist/index.html                   0.98 kB │ gzip:  0.55 kB
dist/assets/index-0GPP043Z.css   30.46 kB │ gzip:  5.85 kB
dist/assets/index-B-UPn0T-.js   214.45 kB │ gzip: 63.02 kB
✓ built in 1.29s
PWA v1.3.0
files generated: dist/sw.js, dist/workbox-9c191d2f.js

Đã xử lý triệt để:
- Local-First Persistent Cache (foodCache) ngăn chặn hoàn toàn hiện tượng dữ liệu chớp mất.
- Hiển thị Tên người dùng trên Header (Khải • Phòng 302 #839201) và Modal Cài đặt.
- Tự động gán created_by: nickname khi thêm món mới.
```


