# C-019 live evidence — 2026-09-04

Closes the remaining Verify item ("On the linked deployment, insert/read through two
requests and verify durable DB rows and reconnect after redeploy") now that C-024/C-025
connected a real hosted `DATABASE_URL` to Production. All requests below hit
`https://sharefridge.vercel.app` (Production) directly via `curl`; this session has no
direct database/service-role credentials, only the public deployed API.

## Insert then independent read (two requests, same durable row)

```
POST /api/auth/create-room {code:"121049",passcode:"1234",nickname:"c019test"} -> 201
  room.id 62ed6ef0-478b-4004-b166-bfa49e7e5861

POST /api/foods {room_code:"121049",name:"C019 durability test",compartment:"FRIDGE_TOP",
                  quantity:"1",shelf_life_days:3}
  -> 201 {"id":"158a38ba-94f7-41c0-a686-b03794cae339", ...}

GET  /api/foods?room_code=121049   (separate, independent request)
  -> 200 {"items":[{"id":"158a38ba-94f7-41c0-a686-b03794cae339", ...}],"total":1}
```

The second request is a fresh HTTP call with no shared in-process state with the first —
the row it returns can only come from the database, not a request-local cache.

Cleaned up: `DELETE /api/foods/158a38ba-94f7-41c0-a686-b03794cae339` -> `{"success":true,...}`.
The empty test room `121049` itself could not be removed this session (no `DELETE /rooms`
endpoint exists in the contract, and this session has no direct DB/service-role access to
delete the row out-of-band) — left behind empty, no food/shopping/subscription rows.

## Reconnect after redeploy

This session did not trigger a new Vercel redeploy (no `vercel` CLI / deploy credentials
available here). Retroactive proof already exists in the repo instead: `evidence/C-024/
live-2026-09-03.md` created room `813074` against one Production deployment, and
`evidence/C-025/live-2026-09-03.md` — run later, against a *different* deployment
(`dpl_CqwppyzN9KhTxyPyhUvhGdi1gRF2`, confirmed by its own live evidence file) — queried
`public.rooms` and found room `813074` still present. That is the same durable-storage
guarantee this Verify item asks for (data surviving a redeploy), already exercised across
two real, distinct deploys rather than manufactured in this session.

## Result

Both remaining acceptance conditions for this Verify item are now satisfied: durable
DB rows via independent HTTP requests (this session), and persistence across a redeploy
(existing cross-session evidence, cited above).
