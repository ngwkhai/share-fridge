# C-025 live evidence — 2026-09-03

Real Supabase Storage bucket confirmed and migrated, deployed to Production, and exercised
end-to-end against the real bucket. All requests below hit
https://sharefridge.vercel.app (Production, `dpl_CqwppyzN9KhTxyPyhUvhGdi1gRF2`), which has no
Deployment Protection, so plain `curl` was used directly.

## Bucket and migration

```
storage.listBuckets() -> [{"name":"food-photos","public":false}]
```

Confirmed via the real `SUPABASE_SERVICE_ROLE_KEY` before configuring anything — a private bucket
already existed with the exact expected name. Ran `npm run db:migrate` against the live project;
`select version from sharefridge_private.schema_migrations` now lists all four versions including
`004_photo_storage` (this had not been applied yet when C-024's migration ran, since
`photos.sql` didn't exist at that point — `/api/config` correctly reported `photos:false` until
this was fixed).

## Config and capability flag

```
GET /api/config -> {"capabilities":{"google":false,"push":true,"photos":true,"realtime":true}}
```

## Real upload, attach, and signed read, live

```
POST /api/auth/create-room {code:650020,...} -> 201, room + room token
POST /api/photos (real 300x200 JPEG, Idempotency-Key: c025-live-1)
  -> 201 {"photo_url":"https://itlgcyuhlvnpfrkaockt.supabase.co/storage/v1/object/sign/food-photos/rooms/650020/85fabdfb-....jpg?token=...","storage_path":"rooms/650020/85fabdfb-....jpg"}
```

Downloaded that exact signed URL directly (not through the app): `HTTP 200, 638 bytes`;
`file` reports `JPEG image data, baseline, precision 8, 300x200, components 3` — a real,
readable image, not a stub.

```
POST /api/foods {room_code:650020,...,storage_path:"rooms/650020/85fabdfb-....jpg"} -> 201
  photo_url present, different signed token than the upload response (freshly signed)
GET  /api/foods?room_code=650020 -> items[0].photo_url present, freshly signed again
```

## Real delete + cron-cleanup, live

```
DELETE /api/foods/{id} -> {"success":true,...}
select storage_path,state from sharefridge_private.photo_uploads where room_code='650020';
--  rooms/650020/85fabdfb-....jpg | pending_delete

GET /api/cron/expiry (correct CRON_SECRET bearer) -> {"success":true,"sent":0,"skipped":0,"failed":0,"pending":0}
select storage_path,state from sharefridge_private.photo_uploads where room_code='650020';
--  rooms/650020/85fabdfb-....jpg | deleted

storage.from('food-photos').list('rooms/650020') -> [] (the real object is gone)
```

Photo cleanup ran inside the same cron invocation used by C-024's scheduler and did not change
the push cron's own result shape (`sent/skipped/failed/pending` all stayed `0`, matching what the
room actually had pending) — confirms "cleanup does not alter push delivery counters".

## Cleanup

Deleted the test room (`650020`); cascades removed its food/subscription rows.
`select count(*) from public.rooms` afterward showed only the operator's own real room (`813074`,
from C-024's live test), confirming no test data was left behind and nothing else was touched.

## Still open

- No two-device side-by-side photo view was done with a physical second device (the signed URL
  was verified by direct download instead, which proves the same thing at the HTTP level: any
  client that can reach that URL sees the real image). The operator may still want to visually
  confirm this from their phone at their convenience; it isn't blocking given the direct
  byte-level proof above.
- Real device PWA install-icon verification (does the home-screen icon actually render the real
  logo, not a broken/default icon) was not performed on a physical device this session.
- `scripts/backfill-photos.js --apply` was only dry-run tested against a local disposable
  database with a synthetic legacy row (see the main evidence README) — this Supabase project
  has no legacy inline-base64 photo rows to migrate, so `--apply` was never exercised against
  live Storage. The script's guard behavior (clean failure without credentials) was verified.
