# C-022 live evidence — 2026-09-04

Operator provided a real `GOOGLE_CLIENT_ID`
(`175412711513-uu1tbcc1f3ul29r0d9shj96plok6jjfp.apps.googleusercontent.com`),
configured on Vercel Production and Preview. Deployed and confirmed server-side
config now reports it correctly:

```
GET /api/config -> {"google_client_id":"175412711513-...apps.googleusercontent.com",
  "capabilities":{"google":true,"push":true,"photos":true,"realtime":true}}
```

The login screen now shows the real Google button ("Tiếp tục sử dụng dịch vụ
bằng Google") instead of the earlier "Đăng nhập Google hiện chưa khả dụng"
unavailable message — the app-level "is Google configured" gate now correctly
passes.

## Found blocker (not closed, needs the operator's Google Cloud Console)

With network tracking active from page load, the actual Google Identity
Services iframe request fails:

```
GET https://accounts.google.com/gsi/button?...&client_id=175412711513-...
  -> HTTP 403
```

This is the request that renders/authorizes Google's own sign-in iframe for
this exact origin + client ID pair. A 403 here is Google's standard response
when the calling origin is not in the Client ID's **Authorized JavaScript
origins** list in Google Cloud Console — the app's own fallback button still
renders visually (so it isn't obviously broken to a quick look), but clicking
it would not complete a real sign-in.

**Fix needed** (operator action, Google Cloud Console → APIs & Services →
Credentials → this OAuth 2.0 Client ID → Authorized JavaScript origins): add
`https://sharefridge.vercel.app` exactly (scheme + host, no path, no trailing
slash) and save. Propagation is usually near-instant but can take a few
minutes.

## Still needed after that fix

Once the origin is authorized, this session still cannot complete the
remaining Verify item itself — "Sign in with two real Google accounts... verify
distinct stable IDs, preserved avatar/profile after reload" requires the
operator's own Google account credentials and an actual OAuth consent
interaction, which this session must not perform (per this project's own
"never enter credentials on the user's behalf" boundary, not just a technical
limitation).
