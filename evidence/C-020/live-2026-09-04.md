# C-020 live evidence — 2026-09-04

Closes the remaining Verify item ("Exercise CRUD on the linked deployment through the
browser/client and confirm persisted IDs/status; capture spec URL and network results")
against Production, `https://sharefridge.vercel.app`, driven through the real browser UI
(Chrome automation), not raw curl — this exercises the actual transpiled `src/services/
api.ts` client, matching the card's scope.

## Spec URL

```
GET https://sharefridge.vercel.app/api/openapi.json -> 200, ShareFridge API 2.0.0
```

## Flow exercised through the UI, with captured network results

1. Created room `512054` ("Phòng 302") via the create-room form.
2. Added a food item ("C020 CRUD test food", 2 hop, FRIDGE_TOP, 3-day shelf life) via
   the QuickAdd modal -> appeared correctly in the fridge list with a persisted id.
3. Consumed it ("Đã nấu") -> moved out of the active fridge list, appeared in "Đã dùng"
   (history) and, since add-to-shopping defaults true, in "Cần mua" (shopping).
4. Toggled the shopping item bought ("Đã mua") -> checkbox state persisted.
5. Deleted the history row and the shopping row via their trash icons -> both lists
   went empty in the UI.

Captured network requests (`read_network_requests`), all `200`:

```
PATCH /api/foods/df8e399b-64ee-462b-a91a-cdd75dd87bfa/consume        200
GET   /api/foods?room_code=512054&status=active                     200 (repeated)
GET   /api/foods?room_code=512054&status=consumed                   200 (repeated)
GET   /api/shopping-items?room_code=512054                          200 (repeated)
PATCH /api/shopping-items/084d11cf-3748-4ab0-96ec-6ee598861b0c/toggle 200
DELETE /api/foods/df8e399b-64ee-462b-a91a-cdd75dd87bfa               200
DELETE /api/shopping-items/084d11cf-3748-4ab0-96ec-6ee598861b0c     200
POST  /api/auth/verify-token                                        200 (repeated, session checks)
GET   /api/rooms/512054                                             200 (repeated)
```

Every persisted id returned by a create/consume call (`df8e399b-...` for the food,
`084d11cf-...` for the shopping item) was the exact id used successfully in the
following PATCH/DELETE calls and reflected correctly in the UI state — confirms IDs
and status round-trip correctly end-to-end through the real client, not just the raw
API.

## Cleanup

Deleted the test food and shopping rows via the UI (both confirmed `200`/empty-list).
The empty test room `512054` itself could not be removed — no `DELETE /rooms` endpoint
exists in the contract and this session has no direct DB/service-role access to remove
the row out-of-band; left behind empty, no food/shopping/subscription rows (matches the
same limitation recorded in `evidence/C-019/live-2026-09-04.md`).
